enum UiStateStatus { success, failure, loading, noResults, noMoreResults, loadingMore }
enum FixtureShortStatus { LIVE, FT, AET, PEN, NS }
enum FixturesTab { finished, upcoming }
enum FixturesScreenHeaderComponentsAnimationStatus { started, completed }